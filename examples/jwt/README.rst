Simple example with JWT
-------------------------

To use this example you need to configure **config.py**
This is a simple example where a user authenticates using Cloudflare Access or Google IAP

- Install it::

	pip install PyJWT
	pip install cryptography


- Run it::

	fabmanager run


That's it!!
